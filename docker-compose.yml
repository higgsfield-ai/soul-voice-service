services:
  soul-voice-service:
    build: .
    image: soul-voice:local
    command: python -m src.main
    restart: unless-stopped
    gpus: all
    init: true
    stop_grace_period: 15m
    volumes:
      - '${VOICE_MODELS_HOST_DIR:-./checkpoints}:/app/checkpoints:ro'
    env_file:
      - .env
